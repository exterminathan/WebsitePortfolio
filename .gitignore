# OS / OneDrive cruft
.DS_Store
Thumbs.db
ehthumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Deploy artifact — resume is added to the deploy source only, never committed (see CLAUDE.md)
resume.pdf

# Build output — generated from src/ by `node build.js`, deployed but not committed
dist/
node_modules/
